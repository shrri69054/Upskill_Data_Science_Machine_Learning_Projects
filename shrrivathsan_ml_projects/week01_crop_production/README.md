# Crop Production Prediction

Place the agricultural CSV at `datafile.csv` or pass `--data path/to/file.csv`.

```bash
pip install pandas scikit-learn joblib
python crop_production_prediction.py --data datafile.csv
```
