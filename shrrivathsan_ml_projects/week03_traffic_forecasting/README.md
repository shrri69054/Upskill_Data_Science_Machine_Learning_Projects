# Smart City Traffic Forecasting

Expected columns include `DateTime`, `Junction`, and a target such as `Vehicles`.

```bash
pip install pandas numpy scikit-learn
python traffic_forecasting.py --data train.csv --target Vehicles
```
